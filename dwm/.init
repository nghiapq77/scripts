#!/bin/sh

PREFIX="$DWM_SCRIPTS/statusbar"
DELIM="|"
battery="$($PREFIX/battery)"
echo $battery
date="$(date +"%b %d (%a) %I:%M")"
status="$battery $DELIM $date"
echo $status

while true; do
    xsetroot -name "$status"
    sleep 1m
done

